<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.micol.lms.board.map.BoardMapper">
	<select id ="boardSelectList" resultType="co.micol.lms.board.service.BoardVO">
		SELECT * FROM BOARD
	</select>

	<select id="boardSelect" resultType="co.micol.lms.board.service.BoardVO">
	
	</select>

</mapper>